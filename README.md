# URL Shortener

A URL shortening service: authenticated users create short links, track clicks, view analytics, and generate QR codes.

> **Project status — foundation.** The monorepo, shared tooling, backend server (health check, request logging, global error handling, auth scaffolding), and a responsive dashboard shell are in place. Link management, redirects, analytics, and QR codes are in progress.

## Tech stack

| Area     | Tools                                                            |
| -------- | ---------------------------------------------------------------- |
| Frontend | React 19, Vite, TanStack Router, TanStack Query, Tailwind CSS v4 |
| Backend  | Hono on Bun, better-auth                                         |
| Database | Neon (PostgreSQL), Drizzle ORM                                   |
| Tooling  | TypeScript, ESLint, Prettier                                     |

End-to-end type safety: the frontend talks to the backend through a Hono RPC client (`frontend/src/lib/client.ts`) that imports the backend router's _type_ across the workspace.

## Prerequisites

- [Bun](https://bun.sh) — package manager and runtime
- A [Neon](https://neon.tech) PostgreSQL database (connection string)

## Getting started

```bash
# 1. Install dependencies for all workspaces
bun install

# 2. Create env files from the examples, then fill in the values
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# 3. Push the database schema (creates the auth tables)
cd backend && bun run db:push && cd ..

# 4. Run the backend (:3000) and frontend (:5173) together
bun run dev
```

Then open <http://localhost:5173>. The API is served at <http://localhost:3000> (health check at `/api/health`).

The two apps also run independently:

```bash
bun run dev:backend    # Hono API on :3000
bun run dev:frontend   # Vite dev server on :5173 (runs without the backend)
```

## Environment variables

### `backend/.env`

| Variable             | Description                                        |
| -------------------- | -------------------------------------------------- |
| `DATABASE_URL`       | Neon PostgreSQL connection string                  |
| `BETTER_AUTH_SECRET` | Auth signing secret (`openssl rand -base64 32`)    |
| `BETTER_AUTH_URL`    | Backend base URL (e.g. `http://localhost:3000`)    |
| `FRONTEND_URL`       | Allowed CORS origin (e.g. `http://localhost:5173`) |
| `PORT`               | Backend port (e.g. `3000`)                         |

### `frontend/.env`

| Variable       | Description                                     |
| -------------- | ----------------------------------------------- |
| `VITE_API_URL` | Backend base URL (e.g. `http://localhost:3000`) |

## Scripts

Run from the repo root:

| Command                | Description                 |
| ---------------------- | --------------------------- |
| `bun run dev`          | Backend + frontend together |
| `bun run dev:backend`  | Backend only (:3000)        |
| `bun run dev:frontend` | Frontend only (:5173)       |
| `bun run lint`         | ESLint across the repo      |
| `bun run format`       | Format with Prettier        |
| `bun run format:check` | Check formatting            |

Backend (`cd backend`):

| Command               | Description                 |
| --------------------- | --------------------------- |
| `bun run typecheck`   | Type-check (`tsc --noEmit`) |
| `bun run db:push`     | Push schema to the database |
| `bun run db:generate` | Generate SQL migrations     |
| `bun run db:migrate`  | Apply migrations            |
| `bun run db:studio`   | Open Drizzle Studio         |

Frontend (`cd frontend`):

| Command             | Description                   |
| ------------------- | ----------------------------- |
| `bun run typecheck` | Type-check (`tsc -b`)         |
| `bun run build`     | Type-check + production build |

## Project structure

```text
url-shortener/
├── backend/
│   ├── src/
│   │   ├── index.ts          # Hono app: /api/health, auth mount, error handler
│   │   ├── types.ts          # Hono context typing (user / session)
│   │   ├── db/               # Neon + Drizzle client and schema
│   │   └── lib/              # better-auth config + requireAuth middleware
│   └── tsconfig.json
├── frontend/
│   ├── src/
│   │   ├── components/       # dashboard-layout.tsx (responsive app shell)
│   │   ├── routes/           # file-based routes: /, /login, /dashboard
│   │   ├── lib/              # RPC client, auth client, utils
│   │   └── index.css         # Tailwind v4 + design tokens
│   └── tsconfig*.json
├── eslint.config.mjs
├── tsconfig.base.json        # shared TS config, extended by both apps
└── package.json              # Bun workspaces
```

## Notes

- `frontend/src/routeTree.gen.ts` is generated by the TanStack Router Vite plugin and is git-ignored. It's created on a fresh checkout the first time you run `bun run dev` or `bun run build` — `frontend` type-checking depends on it.
