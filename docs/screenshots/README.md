# Screenshots

Placeholder images referenced by the root [`README.md`](../../README.md#screenshots). Replace each `.svg` with a real capture (PNG works well) — keep the same filename and the links will just work.

| File              | Capture                                                  |
| ----------------- | -------------------------------------------------------- |
| `dashboard.svg`   | The dashboard — your link list with click counts         |
| `create-link.svg` | The create-link form                                     |
| `analytics.svg`   | A link's analytics detail (chart, referrers, breakdowns) |
| `qr-modal.svg`    | The QR code modal                                        |

A short `demo.gif` of the create → share → analytics flow is a nice addition too.
